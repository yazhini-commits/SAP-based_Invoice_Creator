## Packages
html2canvas | Generating PDF from HTML (if needed for export fallback)
jspdf | Generating PDF from HTML (if needed for export fallback)
date-fns | Date formatting and manipulation
lucide-react | Icons

## Notes
- Relying on window.print() for PDF export for higher quality vector output.
- The UI follows a strict single-page application flow with a complex form and a preview mode.
- Using Plus Jakarta Sans for the primary typography.
